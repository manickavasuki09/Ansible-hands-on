---

- hosts: all
  become: true
  tasks:
   - name: remove apache2 package
     apt:
      name: 
        - apache2
        - apache2
        - apache2-bin
        - apache2-data
        - apache2-utils
      state: absent
